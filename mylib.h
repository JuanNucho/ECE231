#ifndef MYLIB_H   // Include guard to prevent multiple inclusion
#define MYLIB_H

int calculate_sum(int arr[], int size);
float calculate_average(int arr[], int size);
float calculate_std(int arr[], int size);

#endif